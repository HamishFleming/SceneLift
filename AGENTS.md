# Agent Instructions

- Treat this repository as a live prototype for local background removal and OBS integration.
- Prefer small, direct changes that preserve the existing CLI and Python API unless a broader refactor is explicitly requested.
- When you give the user implementation guidance, design notes, or other substantive project information, also create or update the relevant file(s) under `docs/*.md` so the same information is captured in the repository docs.
- Keep the docs aligned with the code. If behavior changes, update the corresponding documentation in the same work.
- Preserve the current compatibility shims unless the user asks to remove them.
