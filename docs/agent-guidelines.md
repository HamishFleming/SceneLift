# Agent Guidelines

This repository uses `AGENTS.md` as the primary instruction file for the coding agent.

Important workflow rule:

- Whenever the agent gives the user substantive project information, it should also create or update the relevant Markdown documentation under `docs/*.md`.

This keeps implementation guidance, design decisions, and usage notes synchronized with the codebase.
